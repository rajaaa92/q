
jQuery(document).ready(function() {
    /*
        Background slideshow
    */
    $('.c-form-container').backstretch('<%= asset_path("bg.jpg")%>');

    /*
        Wow
    */
    new WOW().init();
});
